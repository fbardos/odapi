x-default-group: &default-group marts


models:

  # ---------------------------------------------------------------------------
  - name: mart_ogd_api
  # ---------------------------------------------------------------------------
    config:
      group: *default-group

  # ---------------------------------------------------------------------------
  - name: dim_gemeinde_latest
  # ---------------------------------------------------------------------------
    config:
      group: *default-group
      indexes:
        - columns: [gemeinde_bfs_id]
          unique: true
